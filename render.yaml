services:
  - type: web
    name: api-baldas-blog
    runtime: python
    plan: free
    buildCommand: pip install poetry && poetry install --no-root
    startCommand: poetry run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-10000}
    autoDeploy: true
